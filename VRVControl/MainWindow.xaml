<Window x:Class="VRVControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VRVControl"
        xmlns:views = "clr-namespace:VRVControl.View" 
        xmlns:viewModels = "clr-namespace:VRVControl.ViewModel" 
        mc:Ignorable="d"
        Title="MainWindow" Height="217.625" Width="348.202">

    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType = "{x:Type viewModels:GeneralViewModel}">
            <views:GeneralView/>
        </DataTemplate>

        <DataTemplate DataType = "{x:Type viewModels:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "100" />
            <ColumnDefinition Width = "*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name = "NavBar"  VerticalAlignment="Top" RenderTransformOrigin="0.504,0.715">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="221.136"/>
            </Grid.ColumnDefinitions>

            <Button Content = "General" 
            Command = "{Binding NavCommand}"
            CommandParameter = "general" 
            Grid.Row = "0" />

            <Button Content = "Settings" 
            Command = "{Binding NavCommand}" 
            CommandParameter = "settings" 
            Grid.Row = "1" />
        </Grid>

        <Grid x:Name = "MainContent" Grid.Column = "1">
            <ContentControl Content = "{Binding CurrentViewModel}" />
        </Grid>

    </Grid>
</Window>
